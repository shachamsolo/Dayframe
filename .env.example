# Local secrets. Not committed. Never put these in config.toml.
# Process environment wins if the same variable is already set.

DAYFRAME_API_KEY=
# Path to the OAuth Desktop-app JSON (not a Cloud API key). See README "Google Calendar credentials (M4)".
# DAYFRAME_GOOGLE_CLIENT_SECRET=/Users/you/Dayframe/google_client_secret.json
